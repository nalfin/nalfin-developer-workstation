#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

mkdir -p "$HOME/.local/bin"

ln -sf "$ROOT_DIR/bin/ndw" "$HOME/.local/bin/ndw"

echo "✅ ndw installed to ~/.local/bin"
