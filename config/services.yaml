# =============================================================================
# config/services.yaml — Local development services
# =============================================================================

postgres:
  version: 17
  port: 5432
  user: nalfin
  databases:
    - db_evocave

redis:
  port: 6379

adminer:
  port: 8080

mailpit:
  smtp_port: 1025
  ui_port: 8025
