# =============================================================================
# infra/.env.example — Copy this to infra/.env and adjust values
# =============================================================================

# PostgreSQL
POSTGRES_USER=nalfin
POSTGRES_PASSWORD=secret
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# Adminer
ADMINER_PORT=8080

# Mailpit
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025
